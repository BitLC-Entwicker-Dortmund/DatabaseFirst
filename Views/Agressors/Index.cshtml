@model IEnumerable<DatabaseFirst.Models.Agressor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AgressorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AgressorEigenschaft)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AgressorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AgressorEigenschaft)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AgressorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AgressorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AgressorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
