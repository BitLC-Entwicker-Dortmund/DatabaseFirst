@model IEnumerable<DatabaseFirst.Models.Held>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HeldName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HeldEigenschaft)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HeldName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HeldEigenschaft)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HeldId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HeldId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HeldId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
